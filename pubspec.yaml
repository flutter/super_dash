name: dash_run
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: 3.13.2

dependencies:
  bloc: ^8.1.2
  file_selector: ^1.0.1
  flame: ^1.8.2
  flame_behaviors: ^0.2.0
  flame_tiled: ^1.14.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.0
  leap:
    git:
      url: https://github.com/erickzanardo/leap
      ref: main
      path: packages/leap
  path: ^1.8.3

dependency_overrides:
  flame_tiled:
    git:
      url: https://github.com/flame-engine/flame.git
      ref: feat/allow-tsx-images-in-the-same-level
      path: packages/flame_tiled
dev_dependencies:
  bloc_test: ^9.1.2
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
  very_good_analysis: ^5.0.0

dependency_overrides:
  flame:
    git:
      url: https://github.com/flame-engine/flame.git
      path: packages/flame
  flame_tiled:
    git:
      url: https://github.com/flame-engine/flame.git
      path: packages/flame_tiled

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/map/
    - assets/map/tiles/
    - assets/map/objects/
